pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx}"
      run: bun oxlint --fix {staged_files}
      stage_fixed: true
    format:
      glob: "*.{js,jsx,ts,tsx,json,css,html,md,yml,yaml}"
      run: bun oxfmt --write {staged_files}
      stage_fixed: true
